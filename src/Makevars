# Objects to build
OBJECTS = RcppExports.o sgl_to_rgs.o parser.tab.o scanner.o aes.o geom.o cta.o qual.o scale.o keyword.o title.o case.o cgs_order.o cgs_free.o

# All target
all: $(SHLIB)

# Dependencies
parser.tab.o: parser.tab.c parser.tab.h
scanner.o: scanner.c parser.tab.h

